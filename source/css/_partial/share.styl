.share.card{
    margin-top: 16px;
 .share {
      display: flex;
    text-align: center;
    margin: 8px;
    flex-direction: row;
    justify-content: center;
    a {
      margin: 0 8px;
      display: block;
      height: 40px;
      width: 40px;
      svg {
        width: 100%;
        height: 100%;
      }
    }
 }
.copyright {
  background: #ebebeb;
  padding: 5px 15px;
  margin: 0;
  overflow: hidden;
  position: relative;

  i.icon {
    position: absolute;
    right: -15px;
    font-size: 70px;
    color: #bababa;
    z-index: 0;
    top: -15px;
  }

  .text {
    z-index: 10;
    position: relative;
    font-weight: bold;
    color: #37475b;
  }
}

.dark .copyright {
  i.icon {
    color:   #323c4a;
  }

  .text {
    color: rgba(255, 255, 255, 0.859);
  }
}
}
